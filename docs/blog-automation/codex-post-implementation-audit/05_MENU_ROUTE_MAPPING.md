# 05 — Menu / Route Mapping

Source of truth: `web/src/constants/blogCommandCentreNav.js` (inspected directly, not inferred from menu labels). Every leaf below is generated by that file's `BCC_SECTIONS` array, tagged with its actual `treatment` field — this audit does not trust visual presence alone (§4).

Treatment legend: `NEW` = purpose-built BCC page; `SHARED` = embeds/reuses a canonical existing screen (Content Studio, Publishing, Job Monitor, Audit) — no parallel CRUD; `DEEP_LINK` = routes into an existing Intelligence/AI-Control-Centre/Knowledge page; `SCAFFOLD` = a real route behind a real permission check, honestly rendering a "not yet implemented" placeholder rather than fabricated data.

| Section | Leaf | Route (current) | Treatment | Canonical backend |
|---|---|---|---|---|
| Overview | Overview | `/blog-command-centre` | NEW | `BlogOverviewController` (real overview API, no fabricated metrics — shows "Overview API is not available" rather than invented values on failure) |
| Roadmap | Topic Candidates | `/blog-command-centre/roadmap/candidates` | SCAFFOLD | — |
| Roadmap | Scored Roadmap | `/blog-command-centre/roadmap/scored` | SCAFFOLD | — |
| Roadmap | Content Gaps | `/blog-command-centre/roadmap/gaps` | SCAFFOLD | — |
| Roadmap | Refresh Recommendations | `/blog-command-centre/roadmap/refresh` | SCAFFOLD | — |
| Roadmap | Optimizer History | `/blog-command-centre/roadmap/optimizer` | SCAFFOLD | — |
| Roadmap | Topic Clusters | `/blog-command-centre/roadmap/clusters` | DEEP_LINK | → Knowledge Clusters (existing canonical page, not duplicated) |
| Content Pipeline | Briefs | `/blog-command-centre/pipeline/briefs` | SHARED | Content Studio brief embed |
| Content Pipeline | Drafts | `/blog-command-centre/pipeline/drafts` | SHARED | Content Studio draft embed |
| Content Pipeline | Versions | `/blog-command-centre/pipeline/versions` | SHARED | Content Studio version embed |
| Content Pipeline | Editorial Review | `/blog-command-centre/pipeline/editorial` | SHARED | Content Studio editorial embed |
| Content Pipeline | Research | `/blog-command-centre/pipeline/research` | SCAFFOLD | — |
| Verification & Approvals | Verification Queue | `/blog-command-centre/verification` | SCAFFOLD | — |
| Verification & Approvals | Unsupported Claims | `/blog-command-centre/verification/claims` | SCAFFOLD | — |
| Verification & Approvals | Source Review | `/blog-command-centre/verification/sources` | SCAFFOLD | — |
| Verification & Approvals | Risk Review | `/blog-command-centre/verification/risk` | SCAFFOLD | — |
| Verification & Approvals | Media and Images | `/blog-command-centre/verification/media` | SCAFFOLD | — |
| Verification & Approvals | Human Approval | `/blog-command-centre/approvals` | SHARED (`approval.view`) | Real approval workflow embed → `BlogContentApprovalService` |
| Verification & Approvals | Approval History | `/blog-command-centre/approvals/history` | SHARED (`approval.view`) | Same |
| Publishing | Ready to Publish | `/blog-command-centre/publishing/ready` | NEW | `WorkBlockService::executeSchedulePublish` pipeline |
| Publishing | Published Blogs | `/blog-command-centre/published` | SHARED (`publishing.view`) | Canonical Publishing screen — same as `Publishing > Blogs` (no duplicate CRUD) |
| Publishing | Publishing Calendar | `/blog-command-centre/publishing/calendar` | SHARED (`publishing.view`) | Same canonical Publishing API |
| Publishing | Scheduled Blogs | `/blog-command-centre/publishing/scheduled` | SHARED (`publishing.view`) | Same |
| Publishing | Publishing Attempts | `/blog-command-centre/publishing/deployments` | SHARED (`publishing.view`) | `reach_publication_deployments` |
| Publishing | Rollback | `/blog-command-centre/publishing/rollback` | SHARED (`publishing.view`) | `PublicationRollbackService::rollback()` (fixed this session, see F-09) |
| Publishing | Emergency Unpublish | `/blog-command-centre/publishing/unpublish` | NEW | `PublicationRollbackService::unpublish()` (new method, this session) |
| ~~SEO and Indexing~~ | — | `/blog-command-centre/seo/*`, `/blog-command-centre/indexing` | MOVED + REDIRECT | Section left the tab strip in 2026-08 — see "SEO and Indexing relocation" below |
| Analytics | Portfolio Performance | `/blog-command-centre/analytics/portfolio` | NEW | Portfolio 45/35/20 reporting |
| Analytics | Blog Content | `/blog-command-centre/analytics/content` | DEEP_LINK | → `/intelligence/content` (now backed by real `GoogleAnalyticsContentConnector`) |
| Analytics | Search Performance | `/blog-command-centre/analytics/search` | DEEP_LINK | → `/intelligence/search/queries` |
| Analytics | Product Analytics | `/blog-command-centre/analytics/product` | DEEP_LINK | → `/intelligence/content` |
| Analytics | Lead Attribution | `/blog-command-centre/analytics/attribution` | DEEP_LINK | → `/intelligence/attribution` |
| Operations | Queue Monitor | `/blog-command-centre/operations/queue` | SHARED (`job.view`) | Real Job Monitor embed |
| Operations | Failed Jobs | `/blog-command-centre/operations/failed` | SHARED (`job.view`) | Same |
| Operations | Cron History | `/blog-command-centre/operations/cron` | DEEP_LINK | → Worker Status |
| Operations | AI Usage and Costs | `/blog-command-centre/operations/ai-usage` | SHARED (`ai.generate`) | AI Control Centre embed |
| Operations | Provider Health | `/blog-command-centre/operations/provider-health` | SHARED (`ai.generate`) | Same |
| Operations | Provider Routing | `/blog-command-centre/operations/provider-routing` | SHARED (`ai.generate`) | Same |
| Operations | Audit Log | `/blog-command-centre/operations/audit` | SHARED (`audit.view`) | Real audit system embed |
| Settings | Portfolio Mix, Automation Window, Publication Rules, Risk Rules, Verification Thresholds, Storage, SEO Configuration, Notifications | `/blog-command-centre/settings/*` | NEW | `BlogFeatureFlags`, portfolio config API |
| Settings | Provider Routing | `/blog-command-centre/settings/provider-routing` | DEEP_LINK | → AI Control Centre routing |

## SEO and Indexing relocation (2026-08)

Every leaf of the Blog Command Centre's `SEO and Indexing` section was either a `DEEP_LINK` into `/intelligence/*` or a `SCAFFOLD`. In practice that meant clicking a BCC tab redirected the operator out of the Blog Command Centre and into Intelligence — a different surface with its own fifteen-tab strip and its own page title — with no way back except the browser's Back button. The section also read as a rival of the sidebar's `SEO Command Centre`.

The section is now a first-class sidebar destination in the **SEO** block, renamed **Blog SEO and Indexing** (`/blog-seo`, `web/src/constants/blogSeoNav.js` + `web/src/pages/blog-seo/BlogSeoLayout.jsx`). The three former deep-links render their canonical Intelligence pages **in place** under the section's own sub-nav — the same reuse-not-reimplement rule as the `SHARED` treatment above, and the same pattern `/seo-centre` already uses for these components. No new backend, no parallel CRUD.

| Old route | New route | Treatment | Canonical backend |
|---|---|---|---|
| `/blog-command-centre/seo` | `/blog-seo` | REDIRECT | — |
| `/blog-command-centre/seo/search` | `/blog-seo/search-console` | SHARED | `SearchIntelligencePage` (real GSC adapter) |
| `/blog-command-centre/indexing` | `/blog-seo/indexing` | SHARED | `IndexNowOperationsPage` |
| `/blog-command-centre/seo/internal-links` | `/blog-seo/internal-links` | SCAFFOLD | — |
| `/blog-command-centre/seo/cannibalisation` | `/blog-seo/cannibalisation` | SCAFFOLD | — |
| `/blog-command-centre/seo/sitemap` | `/blog-seo/sitemap` | SHARED | `SitemapOverviewPage` |
| `/blog-command-centre/seo/technical` | `/blog-seo/technical` | SCAFFOLD | — |

`BlogSeoLayout` enforces RBAC at the layout level (`blog.view` or `seo.view`) so the direct URL cannot bypass the hidden menu entry — the same F-02 rule `BlogCommandCentreLayout` follows.

**Not behind `isBlogCommandCentreEnabled()`.** That flag rolls out the blog *automation* pipeline (roadmap → drafts → verification → publishing); none of these six leaves touch it. They report Search Console, indexing and sitemap state for blogs that exist in either flag state, so access here is a permission question (`blog.view` / `seo.view`), not a rollout question — and coupling an SEO destination to a blog-pipeline flag would reshape the SEO sidebar block whenever an unrelated subsystem is toggled.

That decision is what forces the redirects' placement: the seven legacy paths are registered **outside** the flag-gated `/blog-command-centre` route block and outside `BlogCommandCentreLayout` (alongside the existing `/blogs/manage` redirect). Nested inside it they would dead-end at the router catch-all whenever the flag is off, and would hit the layout's `blog.view` denial for a `seo.view`-only operator — even though `/blog-seo` itself is reachable in both cases.

## Legacy vs current

| Previous menu | Previous route | Current menu | Current route | Treatment | Verified result |
|---|---|---|---|---|---|
| Marketing > Blog Management | `/blog` (`BlogListPage.jsx` etc.) | Blog Command Centre | `/blog-command-centre` | MIGRATE + REDIRECT | Legacy routes remain reachable (bookmarks preserved) but redirect via `BlogLegacyRedirects.jsx`; legacy create is blocked server-side in `BlogController::store` regardless of frontend state (`BlogFeatureFlags`), so the legacy screen cannot mint independent records once the flag is on. |
| Publishing > Blogs | `/publishing/blogs` | Publishing > Blogs (retained, aliased) | `/publishing/blogs` | ROUTE ALIAS | Confirmed to call the same canonical publishing API/component as BCC Publishing — no duplicate CRUD, no duplicate route/screen. |
| (none) | — | Intelligence, AI Control Centre, Job Monitor | unchanged | DEEP_LINK / SHARED | Reused via embeds/deep-links from BCC rather than re-implemented; one canonical backend per function (§4.2 requirement) confirmed for briefs, drafts, approvals, publishing, audit, job monitor. |

## Duplicate/broken-link/RBAC checks performed

- No duplicate active sidebar entry for blog functionality found beyond the intentional legacy-redirect + canonical-BCC pair.
- No route without a menu entry, and no menu entry pointing at a dead route, found among the leaves above.
- `BlogCommandCentreLayout` now enforces RBAC and the feature flag at the layout level (fixed this engagement, F-02) — a hidden menu item can no longer be reached via direct URL without also passing the same backend permission check.
- No circular redirects found in `BlogLegacyRedirects.jsx`.
