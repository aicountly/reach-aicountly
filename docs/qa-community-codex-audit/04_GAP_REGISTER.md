# Gap Register

Severity: **Critical** (breaks core guarantee / security) · **High** (breaks a
stated requirement) · **Medium** (quality/consistency) · **Low** (polish).

Status: **Open** · **Fixed** · **Fixed (regression-tested)** · **Deferred (ops-side)**.

| # | Severity | Area | Finding | Status | Fix location |
|---|----------|------|---------|--------|---------------|
| G1 | Critical | Public receiver | Dual-stack receiver: legacy `api/reach/v1/community.php` (wired, `ReachAuth`/`REACH_SERVICE_TOKEN`, question creation was a placeholder that never wrote `public_question_id`) coexisted with the fuller, unwired `CommunityReceiverController` (`CommunityReceiverAuth`/`AICOUNTLY_PUBLIC_SITE_*`, matching what `CommunityPublicSitePublisher` actually signs with). Reach's real publisher was signing requests the live endpoint could never authenticate. | Fixed | `aicountly-com/api/reach/v1/index.php` routes `community/*` to `CommunityReceiverController`; legacy file marked deprecated/dead, kept for reference only |
| G2 | Critical | Public schema | `CommunityReceiverController` referenced columns/tables absent from migration `006` (external IDs, publication source/version/status, `community_comments`, `community_answer_revisions`, moderation mirror columns, AI disclosure/`author_type`). | Fixed | `aicountly-com/database/migrations/008_community_receiver_schema.pgsql.sql` |
| G3 | Critical | Public votes | Nothing server-side rejected a vote cast under a bot/official/service-account identity; only silent absence of such identities from normal SSO login prevented it in practice — not an explicit, testable guarantee. | Fixed (regression-tested) | `aicountly-com/includes/community/CommunityService.php::vote()` (`VOTE_FORBIDDEN_ROLES` guard) + `aicountly-com/tests/CommunityServiceVoteGuardTest.php` |
| G4 | High | Public schema | `community_posts`/`community_answers.user_id` was `NOT NULL`, making bot/official authorship structurally impossible without a fake human row. | Fixed | Migration `008` makes `user_id` nullable, adds `official_identity_id` FK + `CHECK (user_id IS NOT NULL OR official_identity_id IS NOT NULL)` |
| G5 | High | Public UX | AI disclosure text was not sourced from the identity's actual `ai_disclosure` field; bot authorship queries used inner joins that would silently drop bot-authored rows once `user_id` became nullable. | Fixed | `CommunityService.php` (`LEFT JOIN` + `COALESCE` author fallback), `community/question.php` (disclosure precedence: identity `ai_disclosure` → template → canonical string) |
| G6 | High | Public UX | No bot profile / contribution-history page existed despite the receiver publishing content under official identities. | Fixed | `aicountly-com/community/bot.php` + routes in `router.php` / `.htaccess`; `CommunityRepository::getPublishedQuestionsByIdentity/getPublishedAnswersByIdentity` |
| G7 | High | CI | Publisher PHPUnit suite was excluded from the default `composer test` script, so the receiver contract had no CI signal. | Fixed | `aicountly-com/composer.json` `test` script now runs both `tests/phpunit.xml` (PublicSite) and `phpunit.xml` (Publisher) |
| G8 | High | Reach↔Public contract | `CommunityPublisherInterface` only covered a subset of receiver operations (no identities, comments, corrections, moderation, reconcile), so Reach could not actually drive the full contract it claims to implement. | Fixed | `CommunityPublisherInterface`/`CommunityPublicSitePublisher`/`MockCommunityPublisher` extended with `createIdentity`, `updateIdentity`, `createComment`, `applyCorrection`, `applyModerationAction`, `getRecord`, `reconcile` |
| G9 | Critical | Reach engagement | `CommunityEngagementIngestionService` inserted columns (`answer_external_id`, ad hoc event types) that do not exist on `reach_community_engagement_events` (real columns: `answer_id`, `question_id`, `deduplication_key`, `event_timestamp`, `bot_filtered`, `validated`); `CommunityAnalyticsService::engagement()` queried `is_validated`/`created_at`, neither of which exist. Both were 100% broken on first real call. Bot-filtering was a placeholder. No controller/job called this service, so the bug was latent/unreachable rather than actively firing in production — but any future wiring would have failed immediately. | Fixed | `CommunityEngagementIngestionService.php` (real columns, real event-type CHECK vocabulary, real bot-source filter), `CommunityAnalyticsService::engagement()` (correct columns), regression test `tests/Unit/Community/CommunityEngagementValidationTest.php` rewritten to assert against the corrected logic (previous version tested a fictional inline reimplementation that didn't match either the old or new schema, masking the bug) |
| G10 | High | Reach publish gate | `approve()` already correctly gated tier 4/tier 2-3 at approval time, but `OfficialAnswerPublishingService::publish()`/`retryDeployment()` only re-verified the approval checksum, not the risk tier — `setRiskTierWithOverride()` can raise the tier after approval without revoking it, so a since-escalated answer could still publish. | Fixed (regression-tested) | `OfficialAnswerPublishingService::assertPublishableRiskState()`; `tests/Unit/Community/CommunityRiskTierTest.php` (new, 13 tests — this enum had zero prior coverage) |
| G11 | High | Reach reconciliation | `CommunityPublicationReconciliationService` (claimed by `docs/architecture/REACH_COMMUNITY_ARCHITECTURE.md`, "detect state drift between Reach and public site") did not exist; only single-answer on-demand verification existed. `CommunityDeploymentController` was also missing the `reconcile` action the same doc claims. | Fixed (regression-tested) | `CommunityPublicationReconciliationService` + `CommunityPublicationReconciliationJob` + `CommunityDeploymentController::reconcile()` + route; `tests/Unit/Community/CommunityPublicationReconciliationServiceTest.php` (5 tests) |
| G12 | Medium | Reach domain hygiene | Confirm `ContentCommunityDetailModel`/Content Studio remains unreferenced by Community Q&A (no 4th Community domain). | Verified — no gap | Grep-confirmed: only self-referenced, zero callers anywhere else in `server-php/app`. No code change made. |
| G13 | Medium | Reach frontend | Menu/frontend dead buttons, mock data, empty/error states in `CommunityLayout` — not yet re-verified in this pass. | Verified — no gap | `web/src/pages/community/CommunityLayout.jsx` — 8 sub-nav items, all route to real, implemented pages; no fake hierarchy, no dead links found. No code change made. |
| G14 | High | Agent runtime | Role-routed `CommunityOperationalAgentService` (Decision 2A) for the five operational roles + Thread Facilitator limits — not yet implemented at time of writing. | Fixed (regression-tested) | Superseded by G17; see Phase 3 / `11_AGENT_RUNTIME.md` |
| G15 | High | Automation | Community Spark commands, Asia/Kolkata 09:00–19:00 schedule window, queue safety (atomic claim/lease/backoff/DLQ/kill switch) — not yet verified/implemented. | Fixed (regression-tested) | Phase 4 / `12_AUTOMATION_SCHEDULE_QUEUE.md` |
| G16 | Medium | Security/SEO | RBAC route-level re-check, prompt-injection/XSS/SSRF regression tests, QAPage JSON-LD disclosure field, SSO `returnUrl` allowlist re-check — not yet re-verified in this pass. | Fixed (regression-tested) | Phase 5 / `15_RBAC_SSO_SECURITY.md` |
| G17 | High | Agent runtime | No role-routed agent runtime existed at all (Decision 2A). `operational_role` was defined in schema but nothing dispatched on it; `thread_facilitator`/`community_steward`/`question_curator`/`review_objection_desk` had zero runtime behaviour; `expert_answer_assistant` worked only incidentally via pre-existing services unrelated to the role concept. | Fixed (regression-tested) | `CommunityOperationalAgentService` + 4 new per-role collaborators + `CommunityAutomationWindow` + `reach_community_agent_runs`/`reach_community_question_related_links` migrations; see `11_AGENT_RUNTIME.md`. 16 new tests across 3 files. |
| G18 | High | Automation/queue | `CommunityDeploymentModel::listRetryDue()` existed but nothing ever called it — no sweep enqueued `CommunityDeploymentRetryJob` for deployments whose backoff window had elapsed, so a failed publish attempt could only ever be retried by a human manually hitting `/retry`. Separately, `listPendingRetries()` (dead code, unused everywhere) contained a broken `where()` call (`'attempt_count <'` bound to a compiled SELECT string instead of a value). | Fixed | New `CommunityDeploymentRetrySweepJob` (`reach.community_deployment_retry_sweep`), wired into `CommunitySchedule`; dead `listPendingRetries()` removed. |
| G19 | High | Automation/sync | `CommunityPublisherInterface` had a receiver-side `/community/changes` endpoint (`CommunityReceiverController::listChanges`) with no corresponding client method — Reach had no way to learn about public-side moderation/status changes on content it wasn't already specifically polling by ID (comments especially: Reach has zero other visibility into their public lifecycle). | Fixed | `CommunityPublisherInterface::getChanges()` + both implementations; new `CommunityChangeSyncService` (detection-only — never overwrites Reach's own status columns) + `CommunitySyncChangesJob`; cursor stored via existing `SettingModel`. |
| G20 | High | Security/RBAC | `CommunityOperationalAgentService::dispatch()` (Phase 3) had no HTTP entry point, no route, and no permission — it was unreachable dead code from any human or scheduled caller. | Fixed | New `community_agent.dispatch` permission (`Config/Permissions.php`, `App\Enums\CommunityPermission`, seeded onto `community_automation_admin`) + `CommunityAgentDispatchController::dispatch()` + `POST /community/agents/dispatch` route, permission-gated. `force_window` is deliberately not accepted from the request body. |
| G21 | Critical | Security/XSS | `community_markdown()` (renders question/answer bodies on the public `question.php` page) built `<a href="$2">` directly from a markdown-link URL capture with no scheme validation. A body containing `[text](javascript:...)` rendered as a real clickable script-executing link — a stored XSS reachable by any user who can post a question or answer. | Fixed (regression-tested) | `community_is_safe_link_url()` allowlists `http(s):`/`mailto:`/relative paths only; anything else falls back to plain escaped text. `tests/CommunityMarkdownXssTest.php` (8 tests). |

This table is updated in place as phases complete; see `13_TEST_AND_BUILD_EVIDENCE.md`
for the exact test runs backing each "Fixed" status and `16_FINAL_VERDICT.md`
for the closing summary.
